# Plan

1. **Draft (done)** — Vite app with list + calendar + hero, data pipeline, D1 schema and Worker, ADRs 0001–0010, `agents.md`, `README.md`.
2. **2027 data (first pass done)** — `data/2027.json`; open gaps in `docs/data-review.md`.
3. **Review (you)** — look at the site (`npm run dev`), decide the open questions in the README, check `docs/data-review.md`.
4. **Deploy** — GitHub repo + Pages; optionally Cloudflare D1 + Worker.
5. **Polish** — ICS export, map view, SVG icons, scheduled date re-checks, translations.
